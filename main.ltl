// ltl-gradient-boost/main.ltl
//
// Gradient-boosted trees (toy, for teaching).
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-gradient-boost v0.1.0")
    io::println("Gradient-boosted trees (toy, for teaching).")
    0
}
