// examples/demo.ltl — runnable demo for ltl_gradient_boost

import std::io
use ltl_gradient_boost as lib

fn main() {
    io::println("ltl_gradient_boost demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}